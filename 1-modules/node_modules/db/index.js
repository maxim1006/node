'use strict';

//типо база данных

var phrases;

exports.connect = () => {
    phrases = require('./ru');
};

exports.getPhrase = (phrase) => {
    if (!phrases[phrase]) {
        throw new Error('Нет такой фразы ' + phrase);
    }

    return phrases[phrase];
};

